linters:
  enable-all: true

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - scopelint
      text: Using the variable on range scope `(tc|tt)` in function literal

    # For golden file
    - path: (file|integration)_test\.go
      linters:
        - gochecknoglobals
      text: "`update` is a global variable"

    # Usually we have table driven tests which make the test function too long.
    - path: _test\.go
      linters:
        - funlen
      text: Function '.+' is too long

    # Auto-generated by cobra
    - path: cmd/(root|gen).go
      linters:
        - gochecknoglobals
      text: "`(root|gen)Cmd` is a global variable"

    # Auto-generated by cobra
    - path: cmd/gen.go
      linters:
        - gochecknoinits
      text: "don't use `init` function"
